//
// RoomSelectPostRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

internal struct RoomSelectPostRequest: Sendable, Codable, Hashable {

    internal enum Queue: String, Sendable, Codable, CaseIterable {
        case now = "now"
        case next = "next"
        case last = "last"
        case replace = "replace"
    }
    /** The parts of the `data` object of the response that are wanted as a list of JsonPath values. This overrides any session filter. See /session/filter for more details. */
    internal var filter: [String]?
    /** Room ID */
    internal var room: String
    /** Opaque string, returned in the response.  Fixed for a session. */
    internal var session: String?
    /** Opaque string, returned in the response. Varies per message. */
    internal var tag: String?
    /** Identifier that specifies what should be played.  The id is obtained either through a browse/link/match/search request or from the current play queue. */
    internal var mediaId: String
    /** If the item is queueable, specifies how it should modify the play queue.  If not specified, last is assumed.  If the item is not queueable, this parameter is ignored. `now` places the item(s) before what is currently playing, stops play and starts playing the new items. `next` places the item(s) after the currently playing item; `last` places the item(s) at the end of the queue; `replace` replaces the whole queue with the given item(s). */
    internal var queue: Queue?

    internal init(filter: [String]? = nil, room: String, session: String? = nil, tag: String? = nil, mediaId: String, queue: Queue? = nil) {
        self.filter = filter
        self.room = room
        self.session = session
        self.tag = tag
        self.mediaId = mediaId
        self.queue = queue
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case filter
        case room
        case session
        case tag
        case mediaId = "media_id"
        case queue
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(filter, forKey: .filter)
        try container.encode(room, forKey: .room)
        try container.encodeIfPresent(session, forKey: .session)
        try container.encodeIfPresent(tag, forKey: .tag)
        try container.encode(mediaId, forKey: .mediaId)
        try container.encodeIfPresent(queue, forKey: .queue)
    }
}

