//
// SessionFilterPostRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

internal struct SessionFilterPostRequest: Sendable, Codable, Hashable {

    /** The parts of the `data` object of the response that are wanted as a list of JsonPath values. This overrides any session filter. See /session/filter for more details. */
    internal var filter: [String]?
    /** Opaque string, returned in the response.  Fixed for a session. */
    internal var session: String
    /** Opaque string, returned in the response. Varies per message. */
    internal var tag: String?
    /** Request path to which to apply the filter. */
    internal var path: String?
    /** A space-separated list of JsonPath expressions to match parts of the `data` node. Unmatched nodes are filtered out.  If an asterisk (*): the filter is removed, so the entire `data` node is returned.  If \"none\": only the enclosing response message is returned and not the `data` node (this can be used to reduce traffic if only an acknowledgement of success or failure is required).  */
    internal var setFilter: [String]?
    /** If set to true then the device will assume that the client is caching the responses for the given path.  All responses to that command and any asynchronous notifications of the same type will only include properties that have changed. */
    internal var incremental: Bool?

    internal init(filter: [String]? = nil, session: String, tag: String? = nil, path: String? = nil, setFilter: [String]? = nil, incremental: Bool? = nil) {
        self.filter = filter
        self.session = session
        self.tag = tag
        self.path = path
        self.setFilter = setFilter
        self.incremental = incremental
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case filter
        case session
        case tag
        case path
        case setFilter = "set_filter"
        case incremental
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(filter, forKey: .filter)
        try container.encode(session, forKey: .session)
        try container.encodeIfPresent(tag, forKey: .tag)
        try container.encodeIfPresent(path, forKey: .path)
        try container.encodeIfPresent(setFilter, forKey: .setFilter)
        try container.encodeIfPresent(incremental, forKey: .incremental)
    }
}

