//
// MetadataDisplayOptionsItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Information on how to display a given item of metadata. */
internal struct MetadataDisplayOptionsItem: Sendable, Codable, Hashable {

    /** Path to indicate the item of metadata referred to.  The path is relative to the data structure returned by /room/play_state and is defined that way to allow the inclusion of metadata from the channel etc. as well as that of the item itself. */
    internal var path: String?
    /** Human-readable string to label the metdata (e.g. \"Album\", \"Artist\").  The title will be returned in the language defined by the current session. */
    internal var title: String?
    /** Flag to indicate that the title should always be displayed, even in limited space now playing screens, in order to make its meaning clear.  Some metadata items may be hard to interpret without an associated label. */
    internal var showTitle: Bool?
    /** Flag to indicate that the value should be shown (as an empty field) even if uninitialised.  If this is false then the client will skip this value and continue with later values in the array until all its available display fields are filled. */
    internal var showBlank: Bool?

    internal init(path: String? = nil, title: String? = nil, showTitle: Bool? = nil, showBlank: Bool? = nil) {
        self.path = path
        self.title = title
        self.showTitle = showTitle
        self.showBlank = showBlank
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case path
        case title
        case showTitle = "show_title"
        case showBlank = "show_blank"
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(path, forKey: .path)
        try container.encodeIfPresent(title, forKey: .title)
        try container.encodeIfPresent(showTitle, forKey: .showTitle)
        try container.encodeIfPresent(showBlank, forKey: .showBlank)
    }
}

